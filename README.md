# ai-address-book
Address book built entirely by AI assistants

## Setup

This project uses Docker containers for both development and execution. We recommend the use of VSCode which will prompt you to install the needed extensions which support building and launching the development container.